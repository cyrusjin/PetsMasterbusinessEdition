<view class="container has-tab-bar">
  <oa-follow-banner />
  <view wx:if="{{sharedLoading}}" class="shared-tip">
    <text class="text-gray">正在加载分享动态…</text>
  </view>
  <view wx:elif="{{sharedMissing}}" class="shared-tip">
    <text class="text-gray">分享的动态不存在或已删除</text>
  </view>
  <view wx:if="{{logs.length > 0}}">
    <view class="daily-item card {{item.fromShare ? 'from-share' : ''}}" wx:for="{{logs}}" wx:key="id" wx:for-index="logIdx">
      <view class="flex-between mb-16">
        <view class="flex-row gap-16">
          <cached-image wx:if="{{item.petPhoto}}" src="{{item.petPhoto}}" mode="aspectFill" custom-class="pet-avatar-small" />
          <view wx:else class="pet-avatar-small">{{item.petName[0]}}</view>
          <view>
            <text class="boarding-name">{{item.petName}}</text>
          </view>
        </view>
        <text class="text-gray daily-time">{{item.time}}</text>
      </view>
      <view class="daily-checks">
        <text class="tag tag-green" wx:for="{{item.checks}}" wx:key="*this" wx:for-item="c">{{c}}</text>
      </view>
      <view class="daily-desc" wx:if="{{item.description}}">{{item.description}}</view>
      <view class="daily-media" wx:if="{{(item.images && item.images.length) || (item.videoItems && item.videoItems.length) || item.videoUrl || item.video}}">
        <view
          class="daily-img-wrap"
          wx:for="{{item.images}}"
          wx:key="*this"
          wx:for-item="img"
          catchtap="onPreviewImage"
          data-log-index="{{logIdx}}"
          data-url="{{img}}"
        >
          <cached-image src="{{img}}" mode="aspectFill" custom-class="daily-img" />
        </view>
        <view
          wx:for="{{item.videoItems}}"
          wx:key="url"
          wx:for-item="videoItem"
          wx:for-index="videoIdx"
          class="daily-video-thumb {{videoItem.coverUrl ? 'has-cover' : ''}}"
          catchtap="onPreviewVideo"
          data-log-index="{{logIdx}}"
          data-url="{{videoItem.url}}"
          data-video-index="{{videoIdx}}"
        >
          <cached-image
            wx:if="{{videoItem.coverUrl}}"
            src="{{videoItem.coverUrl}}"
            mode="aspectFill"
            custom-class="daily-video-cover"
          />
          <view class="video-play-badge">▶</view>
        </view>
      </view>
      <daily-comments
        wx:if="{{!item.commentsHidden}}"
        log-id="{{item.id}}"
        comments="{{item.comments}}"
        disabled="{{item.commentsDisabled}}"
        bind:change="onCommentsChange"
        data-log-index="{{logIdx}}"
      />
    </view>
  </view>
  <view wx:elif="{{!sharedLoading}}" class="empty-state">
    <text class="empty-icon">📸</text>
    <text class="empty-text">{{sharedMissing ? '分享的动态不存在或已删除' : '暂无宠物动态'}}</text>
  </view>
</view>
